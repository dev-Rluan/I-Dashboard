# 인증 (둘 다 설정 시 Basic Auth 활성화)
DASHBOARD_USER=
DASHBOARD_PASS=

# 자동 갱신 주기 (초)
REFRESH_INTERVAL=5

# Docker 내부에서 호스트 /proc, /sys 경로
HOST_PROC=/proc
HOST_SYS=/sys

# Docker Desktop(Windows/macOS) 환경 명시 (host mount 제약 안내용)
# DOCKER_HOST_OS=windows
# DOCKER_HOST_OS=macos
